name: apphost
volumes:
  bichon_data: {name: apphost_bichon_data}
services:
  bichon:
    image: rustmailer/bichon:1.6.2@sha256:1f15a8e3d64589f2a0d6e486004eb54f345741de6e7d3589d961a07c22ca8e00
    container_name: bichon
    restart: unless-stopped
    runtime: runc
    networks:
      traefik-public: {}
    volumes:
      - bichon_data:/data
    environment:
      TZ: Europe/Berlin
      BICHON_HTTP_PORT: 8080
      BICHON_LOG_LEVEL: info
      BICHON_JSON_LOGS: "true"
      BICHON_PUBLIC_URL: https://${BICHON_SUBDOMAIN:-bichon}.${DOMAIN}
      BICHON_ROOT_DIR: /data
    security_opt: ['no-new-privileges:true']
    cap_drop: [ALL]
    read_only: false
    tmpfs:
      - /tmp:size=256m,mode=1777
    deploy:
      resources:
        limits: {cpus: "1.0", memory: 512M}
        reservations: {memory: 128M}
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/api/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bichon.rule=Host(`${BICHON_SUBDOMAIN:-bichon}.${DOMAIN}`)"
      - "traefik.http.routers.bichon.entrypoints=websecure"
      - "traefik.http.services.bichon.loadbalancer.server.port=8080"
      - "traefik.http.routers.bichon.middlewares=authelia-chain@file"
